serial --unit=0 --speed=115200
terminal_input --append serial
terminal_output --append serial
echo "fleetboot: chainloading from (tftp,${pxe_default_server})"
echo "fleetboot: mac=${net_default_mac} cpu=${grub_cpu} platform=${grub_platform}"
configfile (tftp,${pxe_default_server})/jail/${net_default_mac}/${grub_cpu}/${grub_platform}
echo "fleetboot: configfile returned; nothing to boot"
